# Review fixture diffs

Unified diff samples for `lintro review` unit tests. Load via
`load_review_fixture()` in `tests/unit/ai/review/review_fixtures.py`.

- `chunk_workflow_script_test.diff` — workflow + shell script + bats + source/test pairing
- `chunk_workflow_group_*.diff` — workflow grouping edge cases
- `chunk_groups_*.diff` — workflow script reference matching
- `chunk_source_test_ordering_*.diff` — source/test diff ordering
