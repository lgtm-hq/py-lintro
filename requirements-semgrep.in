# Isolated semgrep pin (#2104). Transitives live in requirements-semgrep.txt.
# Recompile (hash-pinned, Python 3.11 floor matching requires-python):
#   ./scripts/ci/compile-semgrep-lock.sh
# Commit the result: CI's semgrep-lock check fails on drift (#2436).
semgrep==1.173.0
