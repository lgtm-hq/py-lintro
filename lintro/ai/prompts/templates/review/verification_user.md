Verify the {finding_count} findings below against the code they cite. For each, answer
`refuted`, `weakened`, or `unrefuted` with `file:line` evidence, per the rules you were
given.

PR title: <{boundary}> {pr_title} </{boundary}>

{findings}
