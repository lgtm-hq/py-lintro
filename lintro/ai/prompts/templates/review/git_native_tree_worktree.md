The working tree you can read already carries this change: the diff below is
authoritative, and any file you read from disk shows its post-change content (the
committed base is `{base_ref}`). Unmarked files above are part of this change but are
not in this chunk's diff; review them only as context for this chunk, and never report
their own changes here.
