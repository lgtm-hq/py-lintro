# SPDX-License-Identifier: MIT
# For license details, see the repository root LICENSE file.
---
name: CI - Tests

'on':
  push:
    branches: [main]
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened, ready_for_review]
  merge_group:
  workflow_dispatch:

permissions: {}

concurrency:
  group: test-ci-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

jobs:
  test:
    # yamllint disable-line rule:line-length
    uses: lgtm-hq/lgtm-ci/.github/workflows/reusable-test-python.yml@78a5255d51dc2e1e061e4103a95f3ca0c646275d # v0.32.1
    with:
      publish-test-summary: true
      python-versions: '3.11,3.14'
      test-path: tests
      extras: 'full'
      extra-args: '-x --ignore=tests/integration'
      coverage: true
      coverage-format: xml
      coverage-threshold: 80
      upload-coverage: true
      draft-pr-skip: true
      job-name: Python Compatibility
      runner-image: ubuntu-24.04
      tooling-ref: '78a5255d51dc2e1e061e4103a95f3ca0c646275d' # v0.32.1
      egress-policy: block
      egress-preset: pypi
      allowed-endpoints-mode: append
      allowed-endpoints: >
        astral.sh:443
        releases.astral.sh:443
    permissions:
      # v0.18.2 aggregate job needs actions:write for artifact merge
      actions: write
      contents: read
      pull-requests: write

  # Org ruleset (checks-py-lintro) requires this exact check name.
  test-suite-coverage:
    needs: test
    if: >-
      always() &&
      (
        github.event_name != 'pull_request' ||
        github.event.pull_request.draft == false
      )
    # yamllint disable-line rule:line-length
    uses: lgtm-hq/lgtm-ci/.github/workflows/reusable-required-check.yml@78a5255d51dc2e1e061e4103a95f3ca0c646275d # v0.32.1
    permissions:
      contents: read
    with:
      tooling-ref: '78a5255d51dc2e1e061e4103a95f3ca0c646275d' # v0.32.1
      job-name: '🧪 Test Suite & Coverage'
      runner-image: ubuntu-24.04
      upstream-result: ${{ needs.test.result }}
      passed-output: ${{ needs.test.outputs.passed }}
      egress-policy: block
      egress-preset: github-tooling

  publish-coverage:
    name: Publish Coverage to Pages
    needs: test
    if: >-
      github.ref == 'refs/heads/main' &&
      github.event_name == 'push' &&
      needs.test.outputs.passed == 'true'
    # yamllint disable-line rule:line-length
    uses: lgtm-hq/lgtm-ci/.github/workflows/reusable-test-python-publish.yml@78a5255d51dc2e1e061e4103a95f3ca0c646275d # v0.32.1
    with:
      coverage-percent: ${{ needs.test.outputs.coverage-percent }}
      upload-coverage: true
      job-name: Deploy Coverage to Pages
      runner-image: ubuntu-24.04
      tooling-ref: '78a5255d51dc2e1e061e4103a95f3ca0c646275d' # v0.32.1
      egress-policy: block
      egress-preset: github-pages
    permissions:
      contents: read
      pages: write
      id-token: write
