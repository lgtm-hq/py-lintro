# SPDX-License-Identifier: MIT
# For license details, see the repository root LICENSE file.
---
name: Security - SBOM Generation

'on':
  push:
    branches: [main]
  workflow_dispatch:

permissions: {}

concurrency:
  group: sbom-main-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

jobs:
  sbom:
    # yamllint disable-line rule:line-length
    uses: lgtm-hq/lgtm-ci/.github/workflows/reusable-sbom.yml@46cd4f431a6501230b629433c690bbc45d6c01ac # v0.59.28
    with:
      target: .
      target-type: dir
      sbom-format: cyclonedx-json
      scan-vulnerabilities: true
      create-attestation: true
      tooling-ref: '46cd4f431a6501230b629433c690bbc45d6c01ac'  # v0.59.28
      egress-policy: block
      egress-preset: sbom
    permissions:
      # write: reusable-sbom v0.52.3 requests contents: write (release SBOM
      # asset upload); a lower caller grant is a parse-time startup_failure
      contents: write
      security-events: write
      id-token: write
      attestations: write
